# AWS SDK for Python - Already included in Lambda runtime
# boto3>=1.34.0
# botocore>=1.34.0

# No additional dependencies required!
# The Lambda function uses:
# - boto3 (pre-installed in AWS Lambda Python runtime)
# - json (Python standard library)
# - datetime (Python standard library)
# - os (Python standard library)

# Optional: For local testing only
# boto3==1.34.34
# anthropic==0.8.0  # If you want to use Anthropic API directly
# openai==1.0.0     # If you want to use OpenAI instead
